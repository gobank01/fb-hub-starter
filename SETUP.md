# ติดตั้งทีละขั้น

ทำตามลำดับ อย่าข้าม — โดยเฉพาะขั้นที่ 5 (ซ้อมก่อนใช้จริง)

---

## 0. เตรียมเครื่อง

เครื่องอะไรก็ได้ที่เปิดค้างได้ 24 ชม. **ห้ามใช้เครื่องที่คุณใช้ทำงานประจำ**

ตั้งไม่ให้หลับ:
```bash
sudo pmset -a sleep 0 disablesleep 1
```
เช็คว่าได้:
```bash
pmset -g | grep sleep
```
ต้องเห็น `sleep 0`

ติดตั้งของที่ต้องมี:
```bash
brew install node python@3.12
```
แล้วติดตั้ง [Claude Code](https://claude.com/claude-code) และ **ล็อกอินบนเครื่องนั้น** (สำคัญ — SSH เข้าไปสั่งเฉย ๆ อ่าน Keychain ไม่ได้)

---

## 1. ติดตั้ง

```bash
git clone https://github.com/<คุณ>/fb-hub-starter.git
cd fb-hub-starter && bash install.sh
```

---

## 2. ใส่ข้อมูลตัวเอง

```bash
nano ~/fb-hub/.env
```

สองบรรทัดที่ห้ามผิด:

| | |
|---|---|
| `FB_HANDLE` | ชื่อในลิงก์โปรไฟล์ `facebook.com/<อันนี้>` |
| `FB_NAME` | **ชื่อที่ Facebook แสดงบนคอมเมนต์ของคุณ ต้องตรงเป๊ะทุกตัวอักษร** |

`FB_NAME` ผิด = บอทแยกไม่ออกว่าคอมเมนต์ไหนเป็นของตัวเอง แล้ว**จะไปตอบคอมเมนต์ตัวเอง**

---

## 3. สอนให้มันเขียนเหมือนคุณ

```bash
nano ~/fb-hub/config/voice.md
```

**ก๊อปคอมเมนต์เก่าของคุณเองมาแปะ 10-20 อัน** — ได้ผลกว่าอธิบายเป็นคำพูดเยอะ
ยิ่งใส่เยอะยิ่งเหมือน

---

## 4. ล็อกอิน Facebook ในเบราว์เซอร์ของบอท

```bash
bash ~/fb-hub/bin/chrome-bot.sh
```

หน้าต่าง Chrome จะเด้งมา (โปรไฟล์แยกต่างหาก ไม่ปนกับที่คุณใช้เอง) → ล็อกอิน Facebook ให้เรียบร้อย → **ปล่อยค้างไว้ ห้ามปิด**

### ⚠️ เช็กว่าไม่ได้สวมร่างเพจอยู่

เปิดโพสต์ไหนก็ได้ ดูที่กล่องคอมเมนต์ว่าเขียนว่าอะไร

- ✅ `แสดงความคิดเห็นในชื่อ <ชื่อคุณ>`
- ❌ `แสดงความคิดเห็นในชื่อ <ชื่อเพจ>` ← **คอมเมนต์จะออกในนามเพจ** และบอทจะจับคอมเมนต์ตัวเองไม่เจอ

ถ้าผิด ให้สลับกลับเป็นโปรไฟล์ส่วนตัวก่อน

---

## 5. ซ้อมก่อน — ห้ามข้าม

### 5.1 หาโพสต์เจอไหม
```bash
node ~/fb-hub/bin/fb-recent.js 7
```
ควรได้ JSON รายการโพสต์ + Reels ของคุณใน 7 วัน
**ถ้าได้ `[]`** → `FB_HANDLE` / `FB_NAME` ผิด หรือยังไม่ได้ล็อกอิน

### 5.2 ใครยังไม่ได้ตอบบ้าง
```bash
node ~/fb-hub/bin/fb-pending.js 7
```
ใช้เวลาสักพัก (เปิดทีละโพสต์) — ได้รายชื่อคนที่ยังไม่มีรีพลายจากเรา

### 5.3 ซ้อมตอบแบบไม่ส่งจริง ← **สำคัญที่สุด**

ปิดตัวจับเวลาก่อน ไม่งั้นรอบปกติจะรันจริงระหว่างคุณซ้อม:
```bash
launchctl unload ~/Library/LaunchAgents/com.fbhub.fbwatch.plist
```

สร้างงานซ้อม:
```bash
cat > ~/fb-hub/jobs/dry-$(date +%s).job <<'EOF'
งาน: ทดสอบแบบ DRY RUN — ห้ามพิมพ์ ห้ามกดส่ง ห้ามกดถูกใจ เด็ดขาด

ทำตาม ~/fb-hub/prompts/comment-watch.txt ทุกอย่าง ยกเว้น:
- ข้ามขั้นกดถูกใจ / กดตอบกลับ / พิมพ์ / ส่ง / จด ทั้งหมด
- แทนที่จะส่ง ให้เขียนข้อความที่จะตอบออกมาในรายงานเท่านั้น
- ไม่ต้องหน่วงเวลา

รายงานแบบนี้:
RESULT: DRY <จำนวนคนที่จะตอบ>
[โพสต์ url — สรุปเนื้อโพสต์ 1 บรรทัด]
  - <ชื่อคน> พิมพ์ว่า "<คอมเมนต์เขา>"
    จะตอบว่า: "<ข้อความที่ร่าง>"
ท้ายสุด: มีคำถามที่ต้องให้เจ้าของเพจตอบเองไหม
EOF
```

รอสักพัก แล้วอ่านผล:
```bash
cat ~/fb-hub/jobs/dry-*.out
```

**อ่านทุกข้อความที่มันร่าง** ถามตัวเองว่า:
- อ่านแล้วเหมือนคุณพิมพ์เองไหม
- มันแต่งราคา/จำนวน/วันเวลา ที่ไม่ได้อยู่ในโพสต์ไหม ← เจอบ่อยมาก
- มันตอบแบบเอาไปแปะโพสต์ไหนก็ได้ไหม

ไม่ผ่าน → แก้ `prompts/comment-watch.txt` แล้วซ้อมใหม่ **ซ้อมได้ไม่จำกัด ไม่มีใครเห็น**

### 5.4 เปิดใช้จริง

พอใจแล้วค่อยเปิดตัวจับเวลากลับ:
```bash
launchctl load ~/Library/LaunchAgents/com.fbhub.fbwatch.plist
```

---

## 6. บอกมันว่าโพสต์ไหนต้องแจกอะไร

```bash
nano ~/fb-hub/config/post-notes.tsv
```

หนึ่งบรรทัดต่อโพสต์ คั่นด้วย **Tab**:
```
<url โพสต์>	โพสต์ขายเสื้อ — ใครสนใจให้แปะ https://shop.example/tee ⛔ ห้ามบอกราคา/สต็อกเอง
```

- โพสต์ที่มีบรรทัดในไฟล์นี้ **ถูกกวาดก่อนโพสต์อื่นเสมอ**
- โพสต์ที่ไม่มี → บอทตอบธรรมดา ไม่ขาย ไม่แปะลิงก์ ไม่สัญญาอะไร

---

## 7. เปิดอินบ็อกซ์ด้วย (จะทำหรือไม่ก็ได้)

แก้ `~/fb-hub/prompts/inbox-sweep.txt` — **บล็อก "ข้อมูลสินค้าที่อ้างอิงได้" ต้องใส่ให้ครบ**
ทุกตัวเลขที่บอทพูดได้ต้องอยู่ในบล็อกนั้น ไม่มี = ห้ามพูด

เว้นว่างไว้ก็ได้ บอทจะตอบว่า "ขอเช็กก่อน" ทุกครั้ง ซึ่งปลอดภัยกว่า

แล้วเปิด:
```bash
launchctl load ~/Library/LaunchAgents/com.fbhub.fbinbox.plist
```

---

## 8. ดูว่ามันทำอะไรไปบ้าง

```bash
tail -20 ~/fb-hub/state/progress.log
```
```bash
cat ~/fb-hub/jobs/.status
```

อยากดูจากมือถือ → ทำหน้าเว็บ dashboard เพิ่ม (ดู `dashboard/README.md`)

---

## แก้ปัญหา

| อาการ | ทางแก้ |
|---|---|
| `fb-recent.js` คืน `[]` | ยังไม่ล็อกอิน หรือ `FB_HANDLE` ผิด |
| บอทตอบคอมเมนต์ตัวเอง | `FB_NAME` ไม่ตรงกับที่ Facebook แสดง |
| งานไม่เดินตามเวลา | เครื่องหลับ — `pmset -g \| grep sleep` ต้องได้ `sleep 0` |
| Claude บอก "Not logged in" ตอนสั่งผ่าน SSH | ปกติ — SSH อ่าน Keychain ไม่ได้ ต้องส่งงานผ่านคิว (วางไฟล์ `.job`) |
| Chrome ปิดไป | `bash ~/fb-hub/bin/chrome-bot.sh` |
| รีลสแกนได้ 0 คอมเมนต์ | ใช้ `/watch/?v=<id>` แทน `/reel/<id>` — ดู LESSONS.md ข้อ 2 |
| จด SENT แล้วแต่ไม่มีรีพลายจริง | LESSONS.md ข้อ 1 — ห้ามใช้ "กล่องว่าง" เป็นหลักฐาน |

---

## กฎที่ห้ามละเมิด

1. **เครื่องเดียวเท่านั้นที่ทำงาน Facebook** — สองเครื่องพร้อมกัน = ตอบซ้ำคนเดียวกันบนโพสต์สาธารณะ
2. **ซ้อมทุกครั้งที่แก้ `prompts/`** — และปิดตัวจับเวลาก่อนซ้อม
3. **อ่าน [LESSONS.md](LESSONS.md) ให้จบ** — ทุกข้อคือความเสียหายจริงที่เกิดมาแล้ว
