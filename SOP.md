# SOP — ขั้นตอนปฏิบัติ

เอกสารนี้คือ **"ต้องทำอะไรบ้าง"** เรียงตามลำดับจริง
ส่วนรายละเอียดการติดตั้งอยู่ใน [SETUP.md](SETUP.md) · เหตุผลเบื้องหลังกฎอยู่ใน [LESSONS.md](LESSONS.md)

---

# ส่วนที่ 1 — ติดตั้งครั้งแรก (ทำครั้งเดียว)

ติ๊กไปทีละข้อ อย่าข้าม

### ☐ 1.1 เตรียมเครื่อง

- [ ] หาเครื่อง Mac ที่ **เปิดค้างได้ 24 ชม.** และ **ไม่ใช่เครื่องที่คุณใช้ทำงานประจำ**
- [ ] ต่อจอ + คีย์บอร์ดชั่วคราว (ครั้งแรกต้องแตะเครื่องจริง ไม่มีทางเลี่ยง)
- [ ] ตั้งค่า macOS สร้าง user — **จดชื่อ user ไว้** ต้องใช้ตอน SSH

### ☐ 1.2 เปิดทางเข้าจากเครื่องคุณ

**System Settings → General → Sharing** เปิด 2 อัน:

- [ ] **Remote Login** (SSH) — สั่งงานผ่านคำสั่ง
- [ ] **Screen Sharing** — เห็นหน้าจอจริง ← **ขาดไม่ได้** (ล็อกอิน FB/Claude ต้องใช้)

ถอดจอออกได้แล้ว

> Mac mini/Studio ไม่มีจอ → Screen Sharing จะได้ความละเอียดจิ๋ว
> ซื้อ **HDMI dummy plug** (~100 บาท) เสียบทิ้งไว้ เครื่องจะคิดว่ามีจอต่ออยู่

### ☐ 1.3 หาเครื่องให้เจอจากเครื่องคุณ

อยู่ WiFi/แลนเดียวกันก็พอ **ยังไม่ต้องมี Tailscale**

หาเครื่องที่เปิด SSH ในวง:
```bash
dns-sd -B _ssh._tcp local
```

ไม่เจอชื่อ → สแกน IP ตรง ๆ (แก้ `192.168.1` เป็นวงของคุณ):
```bash
for i in $(seq 1 254); do (nc -z -G1 192.168.1.$i 22 2>/dev/null && echo "192.168.1.$i เปิด SSH" &); done; sleep 12
```

เจอหลายตัว → **ตัวที่ใช่คือตัวที่เปิดทั้ง 22 และ 5900**:
```bash
nc -z -G1 <ip> 5900 && echo "ตัวนี้เปิด Screen Sharing ด้วย = ใช่"
```

- [ ] ได้ IP ของเครื่องแล้ว: `____________`

### ☐ 1.4 เข้าเครื่อง

```bash
ssh <user>@<ip>
```

ตั้งชื่อย่อไว้ จะได้ไม่ต้องจำ IP:
```bash
printf '\nHost fbhub\n  HostName <ip>\n  User <user>\n' >> ~/.ssh/config
```
ต่อไปพิมพ์ `ssh fbhub` พอ

- [ ] `ssh fbhub` เข้าได้
- [ ] `vnc://<ip>` (Finder → Go → Connect to Server) เห็นหน้าจอได้

> **IP เปลี่ยนเองได้** — ไปจองไว้ในเราเตอร์ (DHCP reservation) ไม่งั้นวันดีคืนดีหาไม่เจอ
> อยากคุมจากนอกบ้าน → ค่อยลง [Tailscale](https://tailscale.com) ทีหลัง ไม่กระทบของเดิม

### ☐ 1.5 ลงของที่ต้องมี (ทำบนเครื่องนั้น)

```bash
brew install node python@3.12
```
- [ ] ติดตั้ง Google Chrome
- [ ] ติดตั้ง [Claude Code](https://claude.com/claude-code) แล้ว **ล็อกอินผ่าน Screen Sharing** (SSH ล็อกอินไม่ได้ อ่าน Keychain ไม่ถึง)
- [ ] กันเครื่องหลับ:
  ```bash
  sudo pmset -a sleep 0 disablesleep 1
  ```
  เช็ค: `pmset -g | grep sleep` ต้องได้ `sleep 0`

### ☐ 1.6 ติดตั้ง fb-hub

```bash
git clone https://github.com/gobank01/fb-hub-starter.git
cd fb-hub-starter && bash install.sh
```

### ☐ 1.6b ลงตัวคุม — ทำบน **เครื่องคุณเอง** ไม่ใช่เครื่องบอท

```bash
cd fb-hub-starter && bash install-controller.sh
```
มันจะสแกนหาเครื่องที่เปิดทั้ง SSH และ Screen Sharing ให้ แล้วถามว่าจะคุมเครื่องไหน

- [ ] `fbhub status` จากเครื่องคุณ ต้องเห็นสถานะเครื่องบอท
- [ ] ถ้าถามรหัสทุกครั้ง → `ssh-copy-id <user>@<ip>` ครั้งเดียวจบ

ตั้งแต่นี้ไปคำสั่งทุกตัวในเอกสารนี้ **พิมพ์จากเครื่องคุณได้เลย** ไม่ต้อง ssh เข้าไปก่อน

### ☐ 1.7 ใส่ข้อมูลตัวเอง

```bash
nano ~/fb-hub/.env
```
- [ ] `FB_HANDLE` = ชื่อในลิงก์โปรไฟล์ `facebook.com/<อันนี้>`
- [ ] `FB_NAME` = **ชื่อที่ Facebook แสดงบนคอมเมนต์คุณ ต้องตรงเป๊ะทุกตัวอักษร**

> `FB_NAME` ผิด = บอทแยกไม่ออกว่าคอมเมนต์ไหนของตัวเอง แล้ว**จะไปตอบคอมเมนต์ตัวเอง**

```bash
nano ~/fb-hub/config/voice.md
```
- [ ] **ก๊อปคอมเมนต์เก่าของคุณมาแปะ 10-20 อัน** ← ได้ผลกว่าอธิบายเป็นคำพูดมาก

### ☐ 1.8 ล็อกอิน Facebook (ผ่าน Screen Sharing)

```bash
bash ~/fb-hub/bin/chrome-bot.sh
```
- [ ] Chrome เด้งมา → ล็อกอิน Facebook → **ปล่อยค้างไว้ ห้ามปิด**
- [ ] เปิดโพสต์ไหนก็ได้ ดูกล่องคอมเมนต์ว่าขึ้นว่า **"แสดงความคิดเห็นในชื่อ &lt;ชื่อคุณ&gt;"**
      ถ้าขึ้นชื่อเพจ → สลับกลับเป็นโปรไฟล์ส่วนตัวก่อน

### ☐ 1.9 ซ้อมก่อนใช้จริง ← **ห้ามข้ามเด็ดขาด**

```bash
node ~/fb-hub/bin/fb-recent.js 7    # หาโพสต์เจอไหม
```
- [ ] ได้รายการโพสต์กลับมา (ได้ `[]` = `.env` ผิด หรือยังไม่ล็อกอิน)
- [ ] ซ้อมแบบไม่ส่งจริง (ปิดตัวจับเวลาให้เองอัตโนมัติ):
  ```bash
  fbhub dry
  ```
  รอสักพักแล้วดูผลตามที่มันบอก
- [ ] **อ่านทุกข้อความที่มันร่าง** ถามตัวเอง 3 ข้อ:
  - อ่านแล้วเหมือนคุณพิมพ์เองไหม
  - มันแต่งราคา/จำนวน/วันเวลา ที่ไม่มีในโพสต์ไหม ← **เจอบ่อยมาก**
  - มันตอบแบบเอาไปแปะโพสต์ไหนก็ได้ไหม
- [ ] ไม่ผ่าน → แก้ `~/fb-hub/prompts/comment-watch.txt` แล้วซ้อมใหม่ (ซ้อมได้ไม่จำกัด ไม่มีใครเห็น)

### ☐ 1.10 เปิดใช้จริง

```bash
fbhub on
```
- [ ] รอ 30 นาที แล้ว `fbhub status` ต้องเห็นตัวเลข "ตอบไปวันนี้" ขยับ

---

# ส่วนที่ 2 — งานประจำ

## ทุกวัน (2 นาที)

```bash
fbhub status
```

ดู 3 อย่าง:

| ดูอะไร | ปกติควรเป็น |
|---|---|
| มีบรรทัดใหม่ในรอบ 1-2 ชม.ที่ผ่านมาไหม | มี (ถ้าโพสต์มีคอมเมนต์) |
| มีคำถามที่ต้องคุณตอบเองค้างไหม | ตอบเองซะ บอทจะไม่แตะ |
| ข้อความที่มันตอบ อ่านแล้วยังเหมือนคุณไหม | สุ่มดู 2-3 อัน |

## ทุกสัปดาห์ (10 นาที)

- [ ] เปิดโพสต์จริง 1 โพสต์ **นับจากหน้าเว็บว่ามันตอบจริงกี่คน** เทียบกับ log
      ← อย่าเชื่อ log อย่างเดียว ([LESSONS.md ข้อ 1](LESSONS.md))
- [ ] `cat ~/fb-hub/state/used-replies.txt | sort | uniq -d` — มีข้อความซ้ำไหม
- [ ] เช็คว่ายังล็อกอิน FB อยู่ (เข้า Screen Sharing ดู Chrome)

---

# ส่วนที่ 3 — เมื่อมีโพสต์ใหม่ที่ต้องแจกของ/ขายของ

บอท **จะไม่ขายอะไรเลย** จนกว่าคุณจะสั่ง — โพสต์ที่ไม่ได้สั่งมันจะตอบธรรมดาเฉย ๆ

```bash
nano ~/fb-hub/config/post-notes.tsv
```

หนึ่งบรรทัดต่อโพสต์ คั่นด้วย **Tab**:
```
<url โพสต์>	โพสต์ขายเสื้อ — ใครสนใจให้แปะ https://shop.example/tee ⛔ ห้ามบอกราคา/สต็อกเอง ให้กดลิงก์ดูเอง
```

**เขียนโน้ตยังไงให้ปลอดภัย:**

| ✅ ทำ | ❌ ห้าม |
|---|---|
| ใส่ลิงก์ให้เขากดไปดูเอง | ให้บอทบอกราคา/จำนวนที่เหลือ (เปลี่ยนตลอด บอทจะพูดค่าเก่า) |
| ระบุว่าโพสต์นี้เรื่องอะไร | ปล่อยว่างให้บอทเดาเอง |
| เขียน ⛔ ห้ามอะไรไว้ตรง ๆ | คิดว่าบอทจะเดาใจถูกเอง |

โพสต์ที่มีบรรทัดในไฟล์นี้ **ถูกกวาดก่อนโพสต์อื่นเสมอ** — ใส่แล้วมีผลรอบถัดไปทันที

---

# ส่วนที่ 3.5 — โพสต์ลงกลุ่ม / Reels

**ทุกสคริปต์ไม่กดปุ่มสุดท้ายจนกว่าจะใส่ `--confirm`** — ค่าเริ่มต้นคือหยุดที่ด่านตรวจ

```bash
node ~/fb-hub/bin/fb-group-post.js --group <id> --file clip.mp4 --caption cap.txt
```
รันแบบไม่ใส่ `--confirm` ก่อนเสมอ → มันจะบอกว่าผ่านด่านตรวจไหม แล้วค่อยรันซ้ำพร้อม `--confirm`

**ด่านตรวจที่มันเช็คให้:**

| | |
|---|---|
| สื่อแนบขึ้นจริงไหม | มี preview เล่นได้ ไม่ใช่แค่ชื่อไฟล์ |
| แคปชั่นตรงไหม | เทียบกับไฟล์ต้นฉบับเป๊ะตัวอักษรต่อตัวอักษร |
| สวิตช์โปรโมทปิดไหม | (Reels) เปิดค้าง = **ยิงแอดเสียเงินจริง** |
| มีปุ่มโพสต์ไหม | ไม่มี = หน้าเปลี่ยน อย่าเดา |

ด่านพวกนี้มาจากเคสจริงที่**เผยแพร่คลิปหลุดไป 5 คลิป** เพราะเชื่อว่าอัปโหลดสำเร็จแล้ว

**ก่อนโพสต์ลงกลุ่ม เช็คกฎกลุ่มก่อน:**
```bash
node ~/fb-hub/bin/fb-group-rules.js <groupId> <groupId> ...
```
กลุ่มคอมมูนิตี้ส่วนใหญ่ห้ามขายของ — โดนแอดมินเตะง่ายกว่าโดน Facebook แบนเยอะ

---

# ส่วนที่ 4 — เมื่อพัง

| อาการ | สาเหตุ | ทำอะไร |
|---|---|---|
| log ไม่มีบรรทัดใหม่หลายชั่วโมง | เครื่องหลับ | `pmset -g \| grep sleep` ต้องได้ `sleep 0` |
| `fb-recent.js` คืน `[]` | ล็อกอินหลุด หรือ `.env` ผิด | Screen Sharing เข้าไปดู Chrome |
| บอทตอบคอมเมนต์ตัวเอง | `FB_NAME` ไม่ตรง | แก้ `.env` ให้ตรงเป๊ะ |
| log บอก SENT แต่หน้าเว็บไม่มีรีพลาย | ส่งไม่ออกจริง | [LESSONS.md ข้อ 1](LESSONS.md) — เช็คว่ากฎยืนยันยังอยู่ในพรอมต์ |
| Chrome ปิดไป | เครื่องรีสตาร์ท | `bash ~/fb-hub/bin/chrome-bot.sh` แล้วเช็คว่ายังล็อกอินอยู่ |
| งานค้างไม่เดิน | ล็อกค้าง | `rmdir ~/fb-hub/jobs/.lock` |
| Claude บอก "Not logged in" ตอนสั่งผ่าน SSH | ปกติ ไม่ใช่บั๊ก | SSH อ่าน Keychain ไม่ได้ — ส่งงานผ่านคิว (วางไฟล์ `.job`) |
| log เงียบ แต่ `fbhub status` บอกว่าเปิดอยู่ | **ล็อกอิน FB หลุด** | ระบบจะยิงแจ้งเตือน + ตอบ `RESULT: LOGIN_LOST` → เข้า Screen Sharing ไปล็อกอินใหม่ |

**หยุดบอททันที:**
```bash
fbhub off
```
งานที่กำลังรันอยู่จะปล่อยให้จบเอง ไม่ตัดกลางคัน (ตัดกลางคัน = ข้อความค้างครึ่ง ๆ กลาง ๆ)

---

# ส่วนที่ 5 — ห้ามทำ

1. **ห้ามให้สองเครื่องทำงาน Facebook บัญชีเดียวกันพร้อมกัน**
   → ตอบซ้ำคนเดียวกันบนโพสต์สาธารณะ · ย้ายเครื่องต้อง**ปิดเครื่องเก่าก่อน**เสมอ
2. **ห้ามแก้ `prompts/` แล้วปล่อยไปเลย** → `fbhub dry` ซ้อมก่อน แล้วค่อย `fbhub on`
   (ตัวจับเวลาอ่านไฟล์สดทุกรอบ แก้ปุ๊บมีผลปั๊บ)
3. **ห้ามใส่ราคา/เงื่อนไข ลงในพรอมต์แบบลอย ๆ** → ใส่ในบล็อกที่กำหนดเท่านั้น ไม่มีในบล็อก = บอทห้ามพูด
4. **ห้ามเชื่อรายงานของบอท** โดยไม่เปิดหน้าเว็บดูเอง อย่างน้อยสัปดาห์ละครั้ง
5. **ห้ามใช้กับบัญชีคนอื่น** — เครื่องมือนี้สำหรับดูแลบัญชีตัวเองเท่านั้น

---

## เช็คลิสต์ก่อนบอกว่า "ใช้งานได้แล้ว"

- [ ] `ssh fbhub` เข้าได้ · `vnc://` เห็นหน้าจอได้
- [ ] `pmset -g | grep sleep` = `sleep 0`
- [ ] Chrome เปิดค้าง ล็อกอิน FB อยู่ **และไม่ได้สวมร่างเพจ**
- [ ] `node ~/fb-hub/bin/fb-recent.js 7` คืนรายการโพสต์
- [ ] ซ้อมแบบไม่ส่งจริงผ่านแล้ว อ่านข้อความที่มันร่างครบทุกอัน
- [ ] `voice.md` มีตัวอย่างข้อความจริงของคุณอย่างน้อย 10 อัน
- [ ] เครื่องอื่นที่เคยทำงาน FB บัญชีนี้ **ปิดแล้ว**
